# libvctrl_handler

Fundamental contracts for building a version control system.

This crate defines **only** traits, types, errors, and constants. It contains no concrete implementations. It is the single source of truth for the entire `libvctrl` ecosystem.
